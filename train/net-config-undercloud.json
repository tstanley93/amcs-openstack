{
    "network_config": [
        {
            "type": "linux_bridge",
            "name": "br-ctlplane",
            "ovs_extra": [],
            "members": [
                {
                    "type": "interface",
                    "name": "{{LOCAL_INTERFACE}}",
                    "primary": "true",
                    "mtu": "{{LOCAL_MTU}}",
                    "dns_servers": "{{UNDERCLOUD_NAMESERVERS}}"
                }
            ],
            "addresses": [
                {
                    "ip_netmask": "{{LOCAL_IP}}"
                }
            ],
            "routes": "{{HOST_ROUTES}}",
            "mtu": "{{LOCAL_MTU}}"
        }
    ]
}